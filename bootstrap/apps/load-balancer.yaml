apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: load-balancer
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/TunahanCan/load-balancer.git
    targetRevision: main
    path: apps/load-balancer
  destination:
    server: https://kubernetes.default.svc
    namespace: apps
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
